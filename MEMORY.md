# Vibe-Trading Space Memory

- Independent hosted planner for the public `HKUDS/Vibe-Trading` project.
- Public UI copy is English only.
- Do not add visible primary outbound CTAs; conversion stays on `vibe-trading.space`.
- Full planner generation is paid-gated through `/pricing/` and `/api/checkout`.
- Production completion requires Cloudflare D1, Polar checkout, live HTTPS, apex/www, search submission, report-center registration, and browser flow evidence.
